[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    slow: marks tests that download ML models (deselect with -m "not slow")
    integration: marks end-to-end integration tests using EphemeralClient
